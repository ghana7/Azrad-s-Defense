using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mike_Santos : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int iq = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
