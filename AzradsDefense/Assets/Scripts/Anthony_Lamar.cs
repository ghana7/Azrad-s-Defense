using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Anthony_Lamar : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int iq = 2;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
